spring.application.name=daily-planner-api
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/daily_planner_db}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
